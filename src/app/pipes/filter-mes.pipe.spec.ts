import { FilterMesPipe } from './filter-mes.pipe';

describe('FilterMesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMesPipe();
    expect(pipe).toBeTruthy();
  });
});
